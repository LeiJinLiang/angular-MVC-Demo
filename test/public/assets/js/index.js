/**
 * Created by 桃子 on 2016/1/15.
 */
define([
        'angular',
        'app',
        './route'
        ],function(ng){
        ng.element(document).ready(function(){
                ng.bootstrap(document, ['app']);
        })
        console.log(1);
});
